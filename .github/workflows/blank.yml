name: hello world
on: workflow_dispatch
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: build
        run: echo "one"
  build2:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: build2
        run: echo "two"
  build3:
    runs-on: ubuntu-latest
    needs: build2
    steps:
      - name: build3
        run: echo "three"
  
